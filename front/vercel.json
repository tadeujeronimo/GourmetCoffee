{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "git": { "deploymentEnabled": false },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
